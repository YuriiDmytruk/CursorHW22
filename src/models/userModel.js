class User {
    constructor(firstName, lastName, email, password, isReceive) {
        this.firstName = firstName;
        this.lastName = lastName;
        this.email = email;
        this.password = password;
        this.isReceive = isReceive;
      }
}

export default User